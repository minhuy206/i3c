\chapter{System Requirements and Specifications}
\label{ch:requirements}

% DRAFT BRIEF: concrete, testable spec that Ch.4-5 satisfy and Ch.6-7 verify.
% Format = bullet lists + tables; avoid long prose.
% Sources: phase1_spec_v2.md, outline, spec 07.

\section{Functional requirements}
\brief{Enumerate required behaviours as testable line items: SDR private write, SDR private
read, immediate data transfer ($\le 4$ inline bytes in the CMD descriptor), \iic{}-FM legacy
write/read, START/Sr/STOP generation + bus-idle detection, OD/PP phase switching, ENTDAA
multi-device DAA loop, ENEC/DISEC (broadcast + direct), 32-bit register read/write contract,
response + RX FIFO reporting.}

\tabph{Functional requirements list}{phase1_spec_v2.md}

\section{Out-of-scope features}
\brief{Restate deliberate exclusions with one-line rationale each (IBI, Hot-Join, HDR, FM+,
multi-master, target mode, bus recovery, DCT, other CCCs, full HCI compliance).
Cross-reference \cref{ch:intro}.}

\tabph{Out-of-scope features + rationale}{scope analysis}

\section{Performance targets}
\brief{SCL targets: SDR $\le \SI{12.5}{\mega\hertz}$, \iic{}-FM \SI{400}{\kilo\hertz}.
System clock $\ge \SI{333}{\mega\hertz}$ (single domain) so counter-based SCL timing meets
tSCO and the OD/PP windows. State the default timing-CSR values (T_LOW/T_HIGH counts) that
yield \SI{12.5}{\mega\hertz}, and the derivation.}

\tabph{Performance targets + timing-CSR defaults}{improvements.md, csr_register.sv}

\section{Software-visible interface (32-bit register bus)}
\brief{Define the simple (non-AXI/AHB) register contract: write = \texttt{reg_addr_i[11:0]}
+ \texttt{reg_wdata_i[31:0]} + \texttt{reg_wen_i}; read = \texttt{reg_addr_i[11:0]} +
\texttt{reg_ren_i} giving \texttt{reg_rdata_o[31:0]} + \texttt{reg_ready_o}. Note CMD
descriptor 2-DWORD staging and the data-FIFO windows.}

\figph{Register-bus read/write protocol}
  {single-cycle write handshake + read-with-ready timing on the 32-bit reg bus}
  {csr_register.sv}{WaveDrom or TikZ timing}

\section{Verification requirements}
\brief{Phase-1 closure criteria: directed vseqs per category pass; scoreboard cross-checks
(cmd/resp/rx/csr/dat) clean; SVA bound and silent; regression targets green. List the error
codes the design must generate so the scoreboard can be checked against them.}

\tabph{Generated error-status codes (0x0, 0x4, 0x5, 0x6, 0x7, 0x8, 0xA)}{phase1_spec_v2.md}

% NOTE: only the GENERATED error-code set above is in scope --- do not claim codes the RTL
% never emits.
