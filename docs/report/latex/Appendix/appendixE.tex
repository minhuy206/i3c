\chapter{Trích đoạn kết quả regression và coverage}
\label{app:regression}

% Nguồn: src/verification/logs/regression_summary.txt, logs/regression_results.tsv,
%        coverage_report.txt, sva_coverage_report.txt.
Phụ lục này trích các phần tổng kết từ kết quả gốc làm bằng chứng cho
\cref{ch:results}. Các báo cáo đầy đủ được sinh tự động theo quy trình kiểm chứng.

\section*{Tổng kết regression}

Bảng tổng kết và các dòng đầu của bảng kết quả theo từng test (\texttt{Seed = 1}):

\begin{lstlisting}
Regression summary  (Seed = 1)
total=81  pass=81  fail=0  error=0

seq                                 status  uvm_err  uvm_fatal
csr_reset_defaults_vseq             PASS    0        0
csr_enable_disable_vseq             PASS    0        0
csr_broadcast_header_control_vseq   PASS    0        0
csr_timing_rw_vseq                  PASS    0        0
...                                 (77 test tiep theo deu PASS)
\end{lstlisting}

Phần tổng kết UVM ở cuối log của một test đạt có dạng:

\begin{lstlisting}
--- UVM Report Summary ---
** Report counts by severity
UVM_INFO    :   56
UVM_WARNING :    0
UVM_ERROR   :    0
UVM_FATAL   :    0
\end{lstlisting}

\section*{Tổng kết \FunctionalCoverage{}}

Phần đầu của báo cáo \FunctionalCoverage{} trích xuất từ kết quả regression 81 test qua UCIS:

\begin{lstlisting}
I3C Functional Coverage Report
Tests analysed : 81
Closure bins   : 222/222 (100.0%)
Diagnostic bins: 173/173 (100.0%)
KPI policy     : crosses own closure.
                 component coverpoints are diagnostic
\end{lstlisting}

Với mỗi coverpoint và cross, báo cáo liệt kê từng bin kèm số lần hit và số test kích hoạt, ví dụ:

\begin{lstlisting}
Covergroup : i3c_correlated_coverage::cg_abort_termination
Cross      : cx_cause_point
Ownership  : Closure bins
Coverage   : 12/12  (100.0%)
  [HIT ] hc_abort,ccc                    count=  4 in 1 test(s)
  [HIT ] hc_abort,daa_assigned_address   count=  1 in 1 test(s)
  [HIT ] hc_abort,daa_id                 count=  2 in 1 test(s)
  ...
  [HIT ] reset,preamble                  count= 61 in 21 test(s)
\end{lstlisting}

\section*{Tổng kết SVA}

Trong quá trình chạy regression, không ghi nhận assertion fail nào. Do chưa có
công cụ chuyên dụng để phân tích \gls{sva} coverage,
khóa luận không đưa ra kết luận về mức độ bao phủ hoặc tỷ lệ kích hoạt của các
assertion.
